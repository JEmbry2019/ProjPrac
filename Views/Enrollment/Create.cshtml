@model ProjPrac.Models.Enrollment

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Enrollment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @*Line 16 changed from CamperID to Camper so Enum Label would display properly*@
                <label asp-for="Camper" class="control-label"></label>
                <select asp-for="CamperID" class ="form-control" asp-items="ViewBag.CamperID"></select>
            </div>
            <div class="form-group">
                @*Line 22 changed from MealID to Meal so Enum Label would display properly*@
                <label asp-for="Meal" class="control-label"></label>
                <select asp-for="MealID" class ="form-control" asp-items="ViewBag.MealID"></select>
            </div>
            <div class="form-group">
                @*Line 27 changed from GameID to Game so Enum Label would display properly*@
                <label asp-for="Game" class="control-label"></label>
                <select asp-for="GameID" class ="form-control" asp-items="ViewBag.GameID"></select>
            </div>
            <div class="form-group">
                <label asp-for="Grade" class="control-label"></label>
                @* line replaced with line 32 for Enum to work.<input asp-for="Grade" class="form-control" /> *@
                <select asp-for="Grade" class="form-control" asp-items="Html.GetEnumSelectList<Grade>()"></select>
                <span asp-validation-for="Grade" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

